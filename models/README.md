# Models

This folder contains trained model artifacts:

- `bilstm_model.pth` — BiLSTM weights
- `word2idx.json` — vocabulary mapping
- `distilbert_finetuned/` — DistilBERT checkpoint directory

These files will be added manually later.

